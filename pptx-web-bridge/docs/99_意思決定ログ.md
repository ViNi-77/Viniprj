# 変更履歴・意思決定ログ

| 日付 | 版 | 決定 | 理由 | 担当 | 試験 |
|---|---|---|---|---|---|
| 2026-09-09 | 0.1.0 | リポジトリ構成を backend(FastAPI) / frontend(静的) / schema / config / samples / tests / docs に分割 | 企画書 3 章の層構成に対応。社内 Web 移行時に UI と API を分離できる | Fable 5.1（実装・レビュー兼務） | pytest 35 件、E2E 23 件 合格 |
| 2026-09-09 | 0.1.0 | PPTX 生成器に python-pptx を採用（PptxGenJS は見送り） | Node ランタイムを増やさず起動を単純にする。python-pptx で編集可能な要素をすべて生成できることを確認 | 同上 | `test_pptx_roundtrip_keeps_structure` |
| 2026-09-09 | 0.1.0 | Presentation JSON の単位を pt、原点左上に統一 | EMU と px の双方へ整数倍で変換でき、人が読める | 同上 | `test_units_and_metrics.py` |
| 2026-09-09 | 0.1.0 | HTML 解析は BeautifulSoup（静的 DOM）。Playwright は画像化のみ | 非目標「無制限クロール」を守る。構造解析に JS 実行は不要 | 同上 | `test_html_to_pptx.py` |
| 2026-09-09 | 0.1.0 | 画像は JSON に base64 内包 | 単一ファイルで保存・復元できる（受入条件 6） | 同上 | E2E「JSON 保存・再読込」 |
| 2026-09-09 | 0.1.0 | 未対応要素は unsupported 要素 + 警告に統一 | 受入条件 7「エラー停止しない」 | 同上 | `test_unsupported_element_becomes_box_not_error` |
| 2026-09-09 | 0.1.0 | 生成 PPTX の図形名に `role:id` を埋め込む | 再読込で題名・役割を復元し往復一致を保つ | 同上 | `test_pptx_roundtrip_keeps_structure` |
| 2026-09-09 | 0.1.0 | 見た目優先は編集性優先へ自動フォールバック | Chromium 未導入環境でも出力を止めない | 同上 | 手動（Playwright 無効化） |
| 2026-09-09 | 0.1.0 | UI は素の HTML/JS（React 不採用） | 企画書 3 章の MVP 選択肢。ビルド工程を持たず非エンジニアが起動しやすい | 同上 | Playwright による UI 操作確認 |
| 2026-09-09 | 0.1.0 | ダウンロード名は RFC 5987 で符号化 | 日本語題名で HTTP ヘッダが壊れる不具合（E2E で検出）を修正 | 同上 | `test_import_pptx_and_exports` |
| 2026-09-09 | 0.2.0 | テンプレートを「座標付き部品定義」（cover / content / closing）に拡張し、コーポレート標準スタイルを追加 | 参考画像 3 枚の構成を Web と PPTX の両方へ同じ座標で反映するため | Fable 5.1 | `test_generated_pptx_has_no_template_chrome_after_reread`, `test_template_parts_in_web_bundle_and_pptx` |
| 2026-09-09 | 0.2.0 | 要素判別レポート（種別 / 座標 / pt / 編集可）を API・UI・Web 一式に追加 | 「文字は画像ではなくテキストシェイプ」を数値で検証可能にする指示への対応 | 同上 | `test_report_lists_text_as_shapes_with_numbers` |
| 2026-09-09 | 0.2.0 | 模擬 12 枚 PPTX は python-pptx で生成（PptxGenJS 見送り） | 本体と同じ言語・ライブラリで生成と検査を一気通貫にする | 同上 | `mock-pptx/tests` 15 件 |
| 2026-09-09 | 0.2.0 | UI をイベント委譲 + qcDebug + 進捗バー + ログレベル切替へ改修 | 基本設計基準書 3.1 / 3.3 / 7.1 / 7.3 | 同上 | Playwright UI 操作確認 |
| 2026-09-09 | 0.2.0 | 図形種別に triangle / diamond / arrow_right / parallelogram を追加、表セルにラン書式を保持 | 模擬資料で矩形近似・ヘッダー文字色欠落が出たため | 同上 | `test_mock_deck_parses_without_warnings` |
| 2026-09-09 | 0.2.1 | テーマ色と文字スタイルの継承解決を `pptx_styles.py` に集約（Issue #4, #5 を同一周で実装） | 継承チェーンの色解決がテーマ色に依存し分離すると二度手間になるため。1 Issue 1 周の原則の例外として記録 | Fable 5.1 | `tests/test_pptx_styles.py` 5 件 |
| 2026-09-09 | 0.2.1 | brightness は HSL ではなく線形近似 | python-pptx の brightness 値をそのまま使い実装を単純に保つ。誤差は数％ | 同上 | `test_apply_brightness` |
| 2026-09-09 | 0.2.1 | レビュー（別エージェント）の指摘を反映: 継承値に `inherited` 印を付け、テンプレートは継承値のみ上書き | テンプレート題名書式が効かなくなる回帰（高）とテーマ latin フォントの固定（中）を、明示値と継承値の区別で解消 | Fable 5.1（レビュー）→ 実装 | `test_inherited_flags_and_template_override`, `test_inherited_font_not_written_to_pptx_or_html` |
| 2026-09-09 | 0.2.1 | 平行四辺形の傾き = min(w,h) × adj で計算、defaultTextStyle を最終層に追加、clrMapOvr は警告のみ、level>0 は lvl1 へフォールバック（近似） | レビュー指摘（中 / 低）。clrMapOvr の解決と lvl 既定 18pt は影響が小さいため次版 | 同上 | `test_parallelogram_slant_matches_config` |
| 2026-09-09 | 0.2.2 | 手動試験 3 項目を自動化（Issue #11）。大容量の上限は 120 枚で 60 秒 | 例外系・境界値をループ検査に組み込み、回帰を毎周検出する | Fable 5.1 | `tests/test_edge_cases.py` 5 件 |
| 2026-09-09 | 0.2.2 | clrMapOvr をスライド単位の派生テーマ（対応表のみ差し替え）で解決（Issue #14） | 背景反転レイアウトで文字色が逆になる不具合をレビュー指摘から解消 | Fable 5.1 | `test_clr_map_override_resolves_inverted_slide` |
| 2026-09-09 | 0.2.3 | Computed Style を Playwright で取得し、CSS 由来の値は `inherited` 印付きで反映（Issue #6） | 同梱 CSS の配色を活かしつつ、テンプレート適用時は上書きできるようにする。外部通信は route.abort で遮断 | Fable 5.1 | `test_computed_style_*` 3 件 |
| 2026-09-09 | 0.2.3 | 2 周目レビュー反映: HTML 取込を run_in_threadpool で実行（async 上で同期 Playwright が動かない不具合）、描画は JS 無効 + offline、既定色は body の Computed Style と比較、Computed Style の適用範囲を li / カード内文 / figcaption へ拡大 | 高 2 件（API 経由で機能が動かない、取込 HTML のスクリプト実行）を解消し、除外リストのハードコードを排除 | Fable 5.1（レビュー）→ 実装 | `test_computed_style_via_api`, `test_computed_style_ignores_scripts_and_body_defaults`, `test_computed_style_no_network_requests` |
| 2026-09-09 | 0.2.4 | 境界入力の探索（50 種）で見つかった 10 件を修正、判断を `docs/10_デバッグ所見.md` に記録 | 「この状態で良いか」の確認依頼。クラッシュ 0 件、決定性・編集反映・安全性を確認し社内試用可と判断 | Fable 5.1 | `tests/test_robustness.py` 9 件 |
| 2026-09-09 | 0.3.0 | exe 配布（PyInstaller onedir）。Windows 用は GitHub Actions の Windows ランナーでビルド。Chromium は同梱せず Edge / Chrome を channel で使う | 「解凍して exe を起動するだけ」を最優先に。Linux では Windows exe を作れないため CI に委ねる。Chromium 同梱は +150MB で見送り | Fable 5.1 | Linux 版バイナリで取込・出力・見た目優先・再読込を HTTP 経由で確認。Windows は CI のスモークテスト |
| 2026-09-09 | 0.3.0 | 凍結時のパス: 書き込み先は exe の隣、同梱資源は exe の隣 → sys._MEIPASS の順 | 利用者が設定・テンプレート・ロゴ素材を差し替えられ、projects/output/logs を見つけやすい | 同上 | 凍結ビルドの動作確認 |
| 2026-09-09 | 0.3.0 | python-pptx のテンプレートを明示同梱し、`pptx/oxml` ディレクトリを実体として置く | python-pptx が `oxml/../templates` の相対経路で開くため、ディレクトリが無いと Linux で失敗する | 同上 | 凍結ビルドで PPTX 出力成功 |
| 2026-09-10 | 0.3.0 | 文字サイズの決定を `typography.py` に一本化（帯域クランプ、`font_pt`、`font_scale`）。自動縮小は実サイズで出力し normAutofit は書かない | 描画・推定・出力で大きさが食い違い「大小がバラバラ」に見えていた。往復時の二重縮小を避けるため | 同上 | `tests/test_layout_quality.py` |
| 2026-09-10 | 0.3.0 | 画像は実寸（px×0.75）と縦横比を保ち、下限（120pt）を割るなら縮めず次ページへ。本文と隣接する画像は横並び（`layout_hint.row/side`） | 全幅引き伸ばしと列への押し込みで「巨大と極小が同居」していた | 同上 | 同上 |
| 2026-09-10 | 0.3.0 | 分割の前に自動縮小（1.0→0.85）、残り 25% 未満は分割せず送る、続きページに `continuation_of` | 分割が多く「（続き）」だらけで読みにくかった | 同上 | 同上 |
| 2026-09-10 | 0.3.0 | PPTX のトリミングは取込時に焼き込む（保存形式を単純に保つ）。回転は `rotation_deg` | クロップ無視で被写体が小さく見えていた | Web 側で crop を再現する案は HTML 出力側の複雑化が大きいため見送り | 同上 |
| 2026-09-10 | 0.3.0 | 編集キャンバスは、サーバ描画（`web_renderer.slide_html`）の断片を同一オリジンの div に差し込み、選択枠を重ねる方式 | JS へ描画器を移植すると二重実装になる。sandbox iframe ではドラッグ座標の同期が煩雑。オフライン・PyInstaller でビルド工程を増やさない | (a) iframe + postMessage、(b) クライアント描画への全面移植 | `tests/test_api_render.py`、`scripts/ui_smoke.py` |
| 2026-09-10 | 0.3.0 | 利用者が動かした枠には `user_bbox` を付け、テンプレート適用・再配置で上書きしない | 表紙の題名をドラッグしても次の描画でテンプレート位置に戻ってしまうため | 常にテンプレート優先 | 同上 |
| 2026-09-10 | 0.3.0 | テンプレートは利用者の PowerPoint から推定して作る（`template_from_pptx.py`）。推定は規則ベースで、結果は必ず画面で直せる | 固定の AI 生成テンプレートは「自分の様式」にならない。PPTX の表紙・中身・最終ページに既に答えがある | 機械学習で部品を判定 → 学習データが無く説明できないため見送り | `tests/test_template_from_pptx.py`, E2E 4 項目, UI スモーク 7 項目 |
| 2026-09-10 | 0.3.0 | 未保存の提案は `template_kit.use_template()`（contextvar）で描画器・レイアウト・生成器へ差し込む | 保存前に「この解釈で入ります」を見せる必要があり、設定ファイルを経由すると試験や同時利用で混ざる | 資料 JSON にテンプレート定義を埋め込む → スキーマが膨らむため見送り | 同上 |
| 2026-09-10 | 0.3.0 | ユーザーテンプレートは `config/user_templates.json` と `config/template_assets/user/<id>/` に保存（組込の `templates.json` とは分ける。exe 版は exe の隣） | 組込を壊さず、利用者が丸ごとコピーして配れる。`.gitignore` に入れリポジトリを汚さない | 組込ファイルへ追記 → 更新時に上書きされるため見送り | `test_api_from_pptx_preview_save_use_delete` |
| 2026-09-10 | 0.3.0 | レイアウト・マスターのフッター・日付・ページ番号プレースホルダは拾わない。題名・副題・本文はスライドに無いときだけレイアウトのものを位置の手がかりにする | PowerPoint はスライドに置かれたフッター類だけを表示するため、レイアウトの定義を拾うと表示されない部品が増える | 同上 | `test_cover_parts_background_logo_title_footer` |
| 2026-09-10 | 0.3.0 | 中身スライドの流し込みはテンプレートの本文領域（`content.body`）を使い、題名は `content.title` の枠へ置く | 帯・ロゴ・フッターと重ならない位置に本文を置くため。定義が無ければ従来の余白 | 同上 | `test_preview_uses_unsaved_template_and_body_area`, E2E「本文がテンプレートの本文領域に収まる」 |
| 2026-09-10 | 0.3.0 | 「元の PowerPoint を土台にする」出力は任意（既定はオフ）。土台ではレイアウト/マスター由来の部品を描かず、スライド由来の部品だけ描く | マスター・テーマ・フォントを完全一致させたい要望に応えつつ、python-pptx 内部依存のリスクを既定に持ち込まない | 常に土台を使う → 寸法不一致や残骸の問題を全員が負うため見送り | `test_export_with_base_pptx_keeps_master_and_reparses`, E2E「土台 PPTX で出力」 |
| 2026-09-10 | 0.3.0 | `canvas.js` をアダプタ式の工場関数にし、テンプレート画面でも同じ選択・ドラッグ・スナップを使う | 二重実装を避ける。既定インスタンス（`PWB.canvas`）の API は変えない | テンプレート画面用に別のドラッグ実装 → 挙動の差が出るため見送り | `scripts/ui_smoke.py`（既存 13 項目 + 7 項目） |
| 2026-09-10 | 0.3.0 | Copilot 連携は API を使わず、クリップボード・ファイル・貼り付けで受け渡す（`copilot_handoff.py`）。文章と構成だけを渡し、座標とブランド部品はアプリ側とブランドキットに任せる | 社内で使えるのは M365 Copilot のみ（API 利用なし）。「AI は中身、プログラムは決定的な生成」の分担を保つ | Copilot Studio エージェント（別途検討）、外部 LLM API（利用不可） | `tests/test_copilot_handoff.py`, E2E 3 項目, UI スモーク 3 項目 |
| 2026-09-10 | 0.3.0 | Copilot の回答（Markdown）は HTML に変換して既存の `html_parser` に通す。Word 文書は最小の WordprocessingML を自前で書く | 取込器を二重に持たない。python-docx を同梱すると exe が増える | Markdown 専用の取込器 / python-docx 追加 | 同上 |
| 2026-09-10 | 0.3.0 | プロンプトの文面は `config/copilot_prompts.json` に置き、コードに固定しない | 現場で言い回しや枚数の既定を直せるように | 同上 | `test_build_prompt_fills_placeholders_and_brand` |
| 2026-09-10 | 0.3.0 | Copilot エージェント一式（`copilot_agent_kit.py`）は書き出すだけで、登録は利用者が Copilot Studio で手で行う | ここでも API を使わない方針を保つ。エージェント作成の権限はテナント管理側にあり、アプリからは触れない | Graph API でエージェントを作成 → API 利用不可のため見送り | `tests/test_copilot_agent_kit.py`, E2E 1 項目, UI スモーク 1 項目 |
| 2026-09-10 | 0.3.0 | ナレッジは M365 Copilot に合わせて 1 ファイル 30,000 字・目次込み 20 ファイルに収める（別リポジトリの crossai-packager と同じ形。import はせず出力形式だけ合わせる） | 依存を増やさずに、既存の詰め替えツールと相互運用できる形にする。`kit.json` に元の記録を残し、後から別プロファイルへ詰め替えられる | crossai_packager を依存に加える → 別リポジトリのため見送り | `test_index_lists_every_body_file_and_bodies_fit_the_limit` |
| 2026-09-10 | 0.3.0 | 知識の中身は文書ファイルではなく設定とコードから組み立てる | docs/ を同梱しない exe 版でも同じ一式が出る。用途プリセットやテンプレートを直せば知識も追随する | docs/*.md を読んで詰める → exe 版で欠落するため見送り | `test_instructions_and_knowledge_carry_the_format_contract` |
| 2026-09-10 | 0.3.0 | 図解は新しい要素種別を 1 つ（`diagram`）だけ増やし、「型 + 項目」を正として描画・出力の直前にプリミティブへ展開する | 箱・矢印・文字を個別要素で持つと編集も往復も壊れやすい。展開後は既存の描画・生成・検査がそのまま使える | 図解ごとに要素種別を増やす / SmartArt を使う（python-pptx が非対応） | `tests/test_diagrams.py`, E2E 3 項目, UI スモーク 1 項目 |
| 2026-09-10 | 0.3.0 | PPTX では図形名を `diagram:<型>:<id>:<連番>` にして往復で図解へ戻す。戻せない場合は個別図形のまま残す | PowerPoint 側で編集できる図形として出しつつ、取り込み直しでも図解として扱えるようにする。名前が失われても内容は消えない | 図解を画像として貼る（編集できない） | `test_pptx_roundtrip_restores_the_diagram` |
| 2026-09-10 | 0.3.0 | `型: カード` などの Copilot の回答は、従来の「列に分けた文字要素」ではなく図解要素にする | 「HTML 図解風に仕上げる」の目的にはカード図形の方が近く、PowerPoint でも図形として編集できる | 従来どおり layout_hint の列だけにする | `test_markdown_reply_becomes_slides_with_kinds_and_notes`（期待値を更新） |
| 2026-09-10 | 0.3.0 | 図解は分割せず、空きが `layout.diagram_min_height_pt` 未満なら丸ごと次ページへ送る。続きページで何も置けないときは高さを切り詰める | 図解を途中で切ると意味が失われる。あわせて、続きページで送り続ける無限ループ（題名だけが残る場合）を塞いだ | 図解を縦に分割する | `test_layout_places_the_diagram_without_splitting_the_slide` |
| 2026-09-11 | 0.3.0 | 差分マージの対応付けは id ではなく鍵（種別・役割・文字の先頭）で行い、取込時の鍵を要素に焼き付ける（`import_key`） | id は取込のたびに振り直される連番で当てにならない。文字を直しても取込時の鍵が残っていれば対応付けられる | id で対応付ける / 文字の類似度だけで対応付ける | `tests/test_merge.py::test_edits_survive_when_the_import_did_not_change` |
| 2026-09-11 | 0.3.0 | 競合（両方が変わった）は既定で「取り込んだ文言 ＋ 自分の座標・書式」を採用し、競合一覧を画面に出して 1 件ずつ自分の文言に戻せるようにする | 取り込み直す動機は「元ファイルを直したから」が多く、既定は取込側が自然。座標は手で整えたものを壊さない | 常に自分を優先 / 毎回ダイアログで聞く | `test_both_changed_is_reported_as_a_conflict`, UI スモーク |
| 2026-09-11 | 0.3.0 | 前回の取込内容は `meta.import_snapshot` に鍵とハッシュだけで保存する（本文・画像は持たない） | 3 方向マージには「前回どうだったか」が要るが、資料をもう 1 部持つとプロジェクト JSON が倍になる | 取込結果を丸ごと保存 / 保存しない（2 方向マージ） | `test_snapshot_is_small_and_refreshed_after_a_merge` |
| 2026-09-11 | 0.3.0 | 画面右上に版とコミットを出し、`/api/version` で機能一覧も返す | 「更新したのに変わらない」を利用者が自分で切り分けられるようにする。どの段階まで入っているかが一目で分かる | 版だけをログに出す（画面からは分からない） | `tests/test_version.py`, UI スモーク |
| 2026-09-11 | 0.3.0 | JS・CSS の URL に中身から作った印（`?v=`）を付け、HTML 自体は `no-store` で配る | ブラウザが古い JS を握ったままだと、サーバーが新しくても画面は古いまま。印が変わればブラウザは必ず取り直す | 毎回すべて no-store（遅くなる）／利用者に Ctrl+F5 を頼む | `test_page_marks_assets_with_the_build_tag` |
| 2026-09-11 | 0.4.0 | 画面の部品（フォーム・ボタン・dialog・文字寸）は Pico.css 2 を同梱して任せ、自前 CSS はレイアウト（3 ペイン・キャンバス・タブ）だけにする | 自前 CSS は全て px 固定で、ノート PC の 125〜150% 表示で文字とボタンが切れていた。rem 基準の土台に載せ替える方が、個別に直すより再発しない | 自前 CSS を rem に書き直す（同じ崩れが再発しやすい）／ビルドが要るフレームワーク（exe 配布と相性が悪い） | UI スモーク 45 項目（ノート PC 2 構成） |
| 2026-09-11 | 0.4.0 | モーダルは HTML 標準の `<dialog>` にし、保存・キャンセルは固定フッターに置く。本文だけスクロール | 「保存」が見切れてスクロールもできない不具合の原因は、箱の `overflow: hidden` と本文にスクロール領域が無いこと。dialog なら焦点・Esc・背景はブラウザ任せにできる | 自前の overlay div を直す | `laptop_checks`「テンプレート作成の『保存』が見えて押せる」 |
| 2026-09-11 | 0.4.0 | 3 ペインの分割は Split.js（2KB）に任せ、幅は割合で保存して最小幅でクランプする | 自前の splitter は右ペインを 0 にでき、大きい画面で保存した px 幅を小さい画面にそのまま当てていた | 自前 splitter を直す | 手動（境界ドラッグ） |
| 2026-09-11 | 0.4.0 | キャンバスの拡縮は ResizeObserver で自動化する | 部品一覧が伸びた・タブが変わった・進捗が出た、のたびに手で fit() を呼ぶ設計は漏れる | 手動 fit の呼び出しを増やす | 既存 UI スモーク（ドラッグ・リサイズ） |
| 2026-09-11 | 0.4.1 | 帯は「縦横比 4 以上・幅か高さ 30% 以上・端から 15% 以内」の形で判定し、それ以外の塗り図形は装飾として既定では描かない。帯は 3 個まで | 文字の無い塗り図形を全部帯にしていたため、色見本や飾りの四角が 帯 ×8 になり、本文領域を覆っていた | 塗り図形は全部帯（従来）／面積だけで切る | `test_bar_shape_rule`, `test_small_squares_become_decor_not_bars` |
| 2026-09-11 | 0.4.1 | 同じ大きさの塗り図形が 3 個以上並ぶスライドは色見本と見なして部品にせず、色をテーマ候補に回す。中身スライドはプレースホルダと文字図形の点数で自動選択 | 会社テンプレートには色の指定ページが入っていることが多く、それを中身に選ぶと推定が全滅する | 利用者に毎回役割を選ばせる | `test_swatch_slide_is_skipped_and_content_is_the_real_one`, `test_palette_from_swatches_drives_theme_colors` |
| 2026-09-11 | 0.4.1 | 推定結果は「外す」だけでなく、表で役割を変えられるようにする（帯 ⇄ 装飾、文字候補 → 題名/フッター …）。部品に信頼度と一文要約を付ける | 推定は必ず外れる前提で、直す手段が「外す」しか無いと使い物にならない | 推定規則を増やし続ける | `test_set_part_role_moves_parts`, `test_api_part_role` |
