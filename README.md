# dist/windows

GitHub Actions build-windows が作った Windows 用 exe の ZIP。常に最新 1 件のみ（履歴は残さない）。

入手: この ZIP を C:\pptx-web-bridge のような短いパスに展開し pptx-web-bridge.exe を実行（Python 不要）。起動に失敗する場合は logs/startup_error.log を確認。
